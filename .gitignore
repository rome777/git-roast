# 의존성
/node_modules
/.pnp
.pnp.js

# 빌드 산출물
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# 환경 변수 — 절대 커밋하지 않는다 (.env.example 만 올린다)
.env
.env.local
.env.*.local

# 로컬 데이터베이스 (개인 분석 기록이 들어 있다)
/data/*.db
/data/*.db-journal
/data/*.db-wal
/data/*.db-shm

# 로그 / 디버그
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# 에디터 / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# 테스트 / 커버리지
/coverage

.vercel
.env*
!.env.example
